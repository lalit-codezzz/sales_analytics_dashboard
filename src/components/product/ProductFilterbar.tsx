import PriceAndNameFilter from "./PriceAndNameFilter";

export default function ProductFilterbar () {
    return <section className="h-[20%] bg-slate-800 rounded-sm mb-4 p-4">
        <PriceAndNameFilter />
    </section>
}